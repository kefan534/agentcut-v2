# antd v6 + @ant-design/pro-components@3.0.0-beta.3 存在 peer 冲突（pro-components 仍声明 antd ^5）。
# 上游 lockfile 以 legacy-peer-deps 安装生成，故这里固化该选项，保证 Mac / Windows 均可用 `npm install` 一键对齐。
legacy-peer-deps=true
